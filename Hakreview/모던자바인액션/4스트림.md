# 스트림?
현업에서 스트림을 정말 많이 사용한다. Collection 변환시, Entity를 다른 객체와 조합하여 DTO 생성시, Map 만들떄 등등... 어쩔때는 N중첩 스트립을 풀어야 할때도 있다....
앞으로 좀 더 스트림을 잘 다루기 위해 정리한다.
스트림을 이용하면 선언형으로 컬렉션 데이처를 처리할 수 있으며 멀티쓰레드 코드 구현없이 데이터를 병렬처리할 수 있다. 

** java8이전 버전의 코드 
```java
List<Dish> dishes = new ArrayList<>();
for(Dish d : menu){
    if(d.getCalories() <400){
        dishes.add(d);
    }
}
```

** java8이후 버전 
```java
List<Dish> dishes = menu.stream()
        .filter(d -> d.getcalories() <400)
        .collect(toList());

```
내가 보통 사용하는 스트림은 이정도 수준의 스트림이었다. 하지만 조금 더 유용하게 다르기위헤 스트림에 대해 `모던자바인액션` 을 정리하려한다.
스트림이란? 데이터 처리 연산을 지원하도록 소스에서 추출된 연속요소라고 정의 할수 있다. 
- 연속된 요소 : 컬렉션과 마찬가지로 스트림은 특정 요소 형식으로 이루어진 연속된 값 집합의 인터페이스를 제공한다. 
- 소스 : 컬렉션, 배열, 자원 등의 데이터 제공 소스로붙저 데이터를 소비한다. 
- 데이터 처리 연산 : filter, map reduce, find, match, sort 등으로 데이터를 조작할 수 있따.

<b>특징</b>
- 파이프라이닝 : 스트림 연산끼리 연결해서 커다란 파이프라인을 구성할 수 있따. 게으름, 쇼트서킷같은 최적화를 얻을 수 있다.
- 내부반복 

